{
  "name": "coffee-house",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx vite build && npx gh-pages -d dist -e coffee-house"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.104.1",
    "vite": "^8.2.2",
    "vite-plugin-handlebars": "^2.0.3"
  }
}
